class intCollection
{
	public:
		int* array;
		void add(int element);
		int enumerate(int index);
		void remove(int element, bool isOnly);
		void outputElements();
}

class dictionary
{
	
}

