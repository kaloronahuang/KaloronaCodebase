// L.cpp
#include <bits/stdc++.h>

using namespace std;

const int MAX_N = 110;

int T, m, L, ai[MAX_N];

int main()
{
    scanf("%d", &T);
    while (T--)
    {
        scanf("%d%d", &m, &L);
        for (int i = 1; i <= m; i++)
            scanf("%d", &ai[i]);
        // left special;
        // right go;
        for (int i = 1; i <= m; i++)
        {

        }
    }
    return 0;
}